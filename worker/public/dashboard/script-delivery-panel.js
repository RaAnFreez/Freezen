(() => {
  const root = () => document.querySelector('#content');
  const esc = (v) => String(v ?? '—').replace(/[&<>"']/g, (c) => ({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;' }[c]));
  const state = { scripts: [] };
  const css = document.createElement('style');
  css.textContent = `.delivery-page{padding:2px}.delivery-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;flex-wrap:wrap}.delivery-head h2{margin:6px 0}.delivery-head p{margin:0;color:#8d98a8}.delivery-badge{display:inline-flex;padding:7px 10px;border-radius:999px;background:#21152d;color:#c995ff;font-size:11px;font-weight:800}.delivery-new{margin-top:18px;width:100%;min-height:56px;border:0;border-radius:12px;background:#a85cff;color:#fff;font:inherit;font-weight:800}.delivery-grid{display:grid;gap:12px;margin-top:16px}.delivery-card{border:1px solid rgba(255,255,255,.07);border-radius:18px;background:#17131f;padding:16px}.delivery-card-head{display:flex;justify-content:space-between;gap:12px}.delivery-card h3{margin:0;font-size:18px}.delivery-muted{margin-top:5px;color:#858fa0;font-size:12px}.delivery-meta{display:flex;gap:8px;flex-wrap:wrap;margin-top:13px}.delivery-pill{padding:6px 10px;border-radius:999px;background:#14111a;color:#adb7c4;font-size:11px}.delivery-pill.on{background:#12251a;color:#77e8a1}.delivery-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:16px}.delivery-btn{border:0;border-radius:10px;padding:10px 13px;font:inherit;font-weight:700;background:#211927;color:#e1e7ef}.delivery-btn.primary{background:#a85cff;color:#fff}.delivery-empty{padding:60px 18px;text-align:center;border:1px dashed rgba(255,255,255,.08);border-radius:18px;color:#8993a2}.delivery-empty strong{display:block;color:#fff;font-size:18px;margin-bottom:8px}.delivery-modal-bg{position:fixed;inset:0;background:rgba(0,0,0,.68);backdrop-filter:blur(10px);display:flex;align-items:flex-end;justify-content:center;z-index:3500;padding:10px}.delivery-modal{width:min(100%,720px);max-height:96vh;overflow:auto;border:1px solid rgba(255,255,255,.08);border-radius:18px 18px 10px 10px;background:#1c1723;box-shadow:0 30px 100px rgba(0,0,0,.55)}.delivery-modal-head{display:flex;justify-content:space-between;gap:10px;padding:18px 20px;border-bottom:1px solid rgba(255,255,255,.06)}.delivery-modal-head h3{margin:0;font-size:20px}.delivery-modal-head p{margin:4px 0 0;color:#8f99aa;font-size:12px}.delivery-modal-head button{border:0;background:transparent;color:#b6bfca;font-size:25px}.delivery-body{padding:18px 20px}.delivery-field{display:flex;flex-direction:column;gap:8px;margin-bottom:16px}.delivery-field label{font-size:13px;color:#e5ebf3}.delivery-field input,.delivery-field textarea{width:100%;border:1px solid rgba(255,255,255,.08);background:#0e0b12;color:#fff;border-radius:10px;padding:12px;font:inherit}.delivery-field textarea{min-height:120px;resize:vertical}.delivery-info{padding:12px;border:1px solid rgba(168,92,255,.2);border-radius:12px;background:#1d1430;color:#d6b9ee;font-size:12px;line-height:1.5;margin-bottom:16px}.delivery-safe{padding:12px;border:1px solid rgba(72,221,142,.18);border-radius:12px;background:#12251b;color:#83e7aa;font-size:12px;line-height:1.5;margin-bottom:16px}.delivery-footer{display:flex;gap:8px;padding:14px 20px;border-top:1px solid rgba(255,255,255,.06)}.delivery-footer button{flex:1;min-height:46px}.delivery-code{padding:12px;border-radius:12px;background:#0f1814;border:1px solid rgba(72,221,142,.2);margin-top:14px}.delivery-code code{display:block;white-space:pre-wrap;word-break:break-word;color:#dff9e8;font-size:12px}.delivery-version{display:flex;justify-content:space-between;gap:10px;align-items:center;padding:10px 0;border-bottom:1px solid rgba(255,255,255,.05)}@media(min-width:700px){.delivery-modal-bg{align-items:center}.delivery-modal{border-radius:18px}.delivery-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}`;
  document.head.appendChild(css);
  async function api(url, options={}){const headers=new Headers(options.headers||{});headers.set('accept','application/json');if(!(options.body instanceof FormData))headers.set('content-type','application/json');const r=await fetch(url,{credentials:'same-origin',...options,headers});if(r.status===401||r.status===403){location.href='/login';return null;}const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||data.error||`HTTP ${r.status}`);return data;}
  function render(){const el=root();if(!el)return;el.innerHTML=`<section class="delivery-page"><div class="delivery-head"><div><span class="delivery-badge">FREE / KEYLESS</span><h2>Script Delivery</h2><p>Independent public scripts that do not use Service, Provider, Get-Key, or license keys.</p></div></div><div class="delivery-info" style="margin-top:18px"><b>Protection</b><br>Every uploaded version is obfuscated server-side with Frezen Advanced v1.1 / Very High / 100% before storage. The plaintext source is not stored in the Delivery tables.</div><button class="delivery-new" id="delivery-create">＋ Create Free Script</button><div id="delivery-grid" class="delivery-grid"></div></section>`;el.querySelector('#delivery-create').onclick=()=>createModal();renderGrid();}
  function renderGrid(){const grid=document.querySelector('#delivery-grid');if(!grid)return;if(!state.scripts.length){grid.innerHTML='<div class="delivery-empty"><strong>No free scripts</strong><span>Create a Script Delivery entry for your free/keyless script.</span></div>';return;}grid.innerHTML=state.scripts.map(s=>`<article class="delivery-card"><div class="delivery-card-head"><div><h3>${esc(s.name)}</h3><div class="delivery-muted">${esc(s.description||'No description')}</div></div><span class="delivery-pill ${s.status==='ACTIVE'?'on':''}">${esc(s.status)}</span></div><div class="delivery-meta"><span class="delivery-pill">${esc(s.version_count||0)} versions</span><span class="delivery-pill">Active: ${esc(s.active_version||'none')}</span><span class="delivery-pill on">No key</span></div><div class="delivery-actions"><button class="delivery-btn primary" data-act="details" data-id="${esc(s.id)}">Details</button><button class="delivery-btn" data-act="upload" data-id="${esc(s.id)}">Upload Version</button><button class="delivery-btn" data-act="loader" data-id="${esc(s.id)}">Copy Loadstring</button><button class="delivery-btn" data-act="toggle" data-id="${esc(s.id)}">${s.status==='ACTIVE'?'Disable':'Enable'}</button><button class="delivery-btn" data-act="delete" data-id="${esc(s.id)}">Delete</button></div></article>`).join('');grid.querySelectorAll('[data-act]').forEach(b=>b.onclick=()=>action(b.dataset.act,b.dataset.id));}
  async function load(){render();try{const data=await api('/api/v1/script-delivery');state.scripts=Array.isArray(data.scripts)?data.scripts:[];render();}catch(error){const grid=document.querySelector('#delivery-grid');if(grid)grid.innerHTML=`<div class="delivery-empty"><strong>Unable to load Script Delivery</strong><span>${esc(error.message)}</span></div>`;}}
  function modal(content){const bg=document.createElement('div');bg.className='delivery-modal-bg';bg.innerHTML=`<section class="delivery-modal">${content}</section>`;document.body.appendChild(bg);return bg;}
  async function createModal(){const bg=modal(`<div class="delivery-modal-head"><div><h3>Create Free Script</h3><p>No key, service, provider, or Get-Key flow.</p></div><button id="close">×</button></div><div class="delivery-body"><div class="delivery-field"><label>Name *</label><input id="name" placeholder="Free Script"></div><div class="delivery-field"><label>Description</label><input id="description" placeholder="Optional description"></div><div class="delivery-safe"><b>Automatic protection</b><br>The first uploaded version will be obfuscated with Advanced v1.1 / Very High / 100% and only the obfuscated payload will be stored for delivery.</div><div class="delivery-field"><label>Initial Lua file *</label><input id="file" type="file" accept=".lua,text/plain"></div><div class="delivery-field"><label>Version *</label><input id="version" value="v1.0.0"></div></div><div class="delivery-footer"><button class="delivery-btn" id="cancel">Cancel</button><button class="delivery-btn primary" id="create">Create</button></div>`);const close=()=>bg.remove();bg.querySelector('#close').onclick=close;bg.querySelector('#cancel').onclick=close;bg.querySelector('#create').onclick=async()=>{const name=bg.querySelector('#name').value.trim(),description=bg.querySelector('#description').value.trim(),file=bg.querySelector('#file').files?.[0],v=bg.querySelector('#version').value.trim();if(!name||!file||!v)return alert('Name, Lua file and version are required.');const btn=bg.querySelector('#create');btn.disabled=true;btn.textContent='Creating…';try{const created=await api('/api/v1/script-delivery',{method:'POST',body:JSON.stringify({name,description})});const form=new FormData();form.append('file',file);form.append('version',v);form.append('release_notes','Initial protected delivery version');await api(`/api/v1/script-delivery/${encodeURIComponent(created.script.id)}/versions`,{method:'POST',body:form});close();await load();alert('Free Script created. The uploaded source was obfuscated before storage.');}catch(error){alert(`Create failed: ${error.message}`);}finally{btn.disabled=false;btn.textContent='Create';}};}
  async function details(id){try{const data=await api(`/api/v1/script-delivery/${encodeURIComponent(id)}`);const s=data.script,versions=data.versions||[];const loadstring=`loadstring(game:HttpGet("${location.origin}/delivery/${encodeURIComponent(id)}"))()`;const bg=modal(`<div class="delivery-modal-head"><div><h3>${esc(s.name)}</h3><p>Independent keyless delivery</p></div><button id="close">×</button></div><div class="delivery-body"><div class="delivery-safe"><b>Delivery protection</b><br>Stored payload is obfuscated Advanced v1.1 / Very High / 100%. Public endpoint serves only the obfuscated active version.</div><div class="delivery-field"><label>Status</label><div class="delivery-pill ${s.status==='ACTIVE'?'on':''}" style="display:inline-flex;width:max-content">${esc(s.status)}</div></div><div class="delivery-field"><label>Versions</label>${versions.length?versions.map(v=>`<div class="delivery-version"><span><b>${esc(v.version)}</b><div class="delivery-muted">${esc(v.release_notes||'')}</div></span><span><span class="delivery-pill">${esc(v.status)}</span> ${v.status==='ACTIVE'?'':`<button class="delivery-btn" data-activate="${esc(v.id)}">Activate</button>`}</span></div>`).join(''):'<div class="delivery-empty">No versions</div>'}</div><div class="delivery-code"><div class="delivery-muted">Keyless loadstring</div><code id="loadstring">${esc(loadstring)}</code><div class="delivery-actions"><button class="delivery-btn primary" id="copy">Copy Loadstring</button><button class="delivery-btn" id="open">Test URL</button></div></div></div>`);bg.querySelector('#close').onclick=()=>bg.remove();bg.querySelector('#copy').onclick=async()=>{await navigator.clipboard.writeText(loadstring);alert('Loadstring copied.');};bg.querySelector('#open').onclick=()=>window.open(`/delivery/${encodeURIComponent(id)}`,'_blank');bg.querySelectorAll('[data-activate]').forEach(b=>b.onclick=async()=>{try{await api(`/api/v1/script-delivery/${encodeURIComponent(id)}/versions/${encodeURIComponent(b.dataset.activate)}/active`,{method:'PATCH'});bg.remove();load();}catch(error){alert(error.message);}});}catch(error){alert(`Details failed: ${error.message}`);}}
  async function upload(id){const bg=modal(`<div class="delivery-modal-head"><div><h3>Upload Free Script Version</h3><p>Maximum source/output size: 3 MiB.</p></div><button id="close">×</button></div><div class="delivery-body"><div class="delivery-field"><label>Version *</label><input id="version" placeholder="v1.0.1"></div><div class="delivery-field"><label>Lua file *</label><input id="file" type="file" accept=".lua,text/plain"></div><div class="delivery-field"><label>Release notes</label><textarea id="notes"></textarea></div><div class="delivery-safe">The server obfuscates the upload automatically. Plain source is not written to the Delivery tables.</div></div><div class="delivery-footer"><button class="delivery-btn" id="cancel">Cancel</button><button class="delivery-btn primary" id="upload">Upload</button></div>`);const close=()=>bg.remove();bg.querySelector('#close').onclick=close;bg.querySelector('#cancel').onclick=close;bg.querySelector('#upload').onclick=async()=>{const file=bg.querySelector('#file').files?.[0],v=bg.querySelector('#version').value.trim();if(!file||!v)return alert('Version and Lua file are required.');const form=new FormData();form.append('file',file);form.append('version',v);form.append('release_notes',bg.querySelector('#notes').value);try{await api(`/api/v1/script-delivery/${encodeURIComponent(id)}/versions`,{method:'POST',body:form});close();load();alert('Version uploaded and obfuscated. It is archived until activated.');}catch(error){alert(`Upload failed: ${error.message}`);}};}
  async function action(act,id){if(act==='details'||act==='loader')return details(id);if(act==='upload')return upload(id);if(act==='delete'){if(!confirm('Delete this free Script Delivery and all of its free-delivery versions?'))return;try{await api(`/api/v1/script-delivery/${encodeURIComponent(id)}`,{method:'DELETE'});load();}catch(error){alert(error.message);}return;}const item=state.scripts.find(s=>s.id===id);if(!item)return;const next=item.status==='ACTIVE'?'DISABLED':'ACTIVE';try{await api(`/api/v1/script-delivery/${encodeURIComponent(id)}`,{method:'PATCH',body:JSON.stringify({status:next})});load();}catch(error){alert(error.message);}}
  window.FrezenDashboardPanels=window.FrezenDashboardPanels||{};
  window.FrezenDashboardPanels['script-delivery']=load;
})();
