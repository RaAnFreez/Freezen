name = "frezen-control-system-v3"
main = "src/index.js"
compatibility_date = "2026-08-09"
workers_dev = true

[vars]
FREZEN_ENV = "development"

# D1 bindings and production secrets will be added in later phases.
# Never put API keys, passwords, bot tokens, or master secrets here.
